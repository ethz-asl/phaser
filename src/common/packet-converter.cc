#include <packlo/common/packet-converter.h>

namespace common {

model::Packet PacketConverter::convert(
    const ouster_ros::PacketMsgConstPtr& packet) {

  return model::Packet {};
}

} // namespace common

